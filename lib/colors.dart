import 'package:flutter/material.dart';

// Maroon Color
const maroon = Color(0xFF800000);

// White Color
const white = Color(0xFFFFFFFF);

const accent = Color(0xFFFFC107);

// Blue Color
const blue = Color(0xFF1857a4);

// blue grey color
const blueGrey = Color(0xFF607D8B);

// detailColor
const detailColor = Color(0xFF2962FF);
